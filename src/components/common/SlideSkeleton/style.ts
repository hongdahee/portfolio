import styled from "styled-components";

export const SkeletonWrapper = styled.div`
  .skeleton_slide {
    width: 100%;
    height: 19.5vw;
    border-radius: 4px;
    overflow: hidden;
    position: relative;
  }
`;
